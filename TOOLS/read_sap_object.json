{
  "type": "function",
  "function": {
    "name": "read_sap_object",
    "description": "Fetch the source code of an existing SAP object (program, class, method, function module). Use it to view or analyze code, and always as a prerequisite before modify_sap_object or review_sap_code.",
    "parameters": {
      "type": "object",
      "properties": {
        "object_type": {
          "type": "string",
          "enum": ["PROG", "CLAS", "METH", "FUNC", "REPS", "OTHER"],
          "description": "Type of SAP object to read"
        },
        "object_name": {
          "type": "string",
          "description": "Technical name, e.g. 'ZCL_TEST', 'ZCL_TEST=>METHOD_NAME', 'Z_PROGRAM'. Supports a wildcard pattern to list matching objects (e.g. 'ZCL_AITOOL_*') and a comma-separated list to read several objects in one call (e.g. 'ZCL_A, ZCL_B, ZCL_C')"
        },
        "purpose": {
          "type": "string",
          "enum": ["view", "analyze"],
          "description": "Why you are reading. 'view' = the user just wants to SEE/open this exact source and nothing else (it is returned as-is and the task ends). 'analyze' = this read is one step of a bigger task (review, security/vulnerability analysis, explanation, following dependencies, or before a modify) and you will keep working afterwards. If unsure use 'analyze'. Defaults to 'analyze' when omitted."
        }
      },
      "required": ["object_type", "object_name"]
    }
  }
}
